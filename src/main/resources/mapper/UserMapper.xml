<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aigt.code.dao.UserDao">
    
    <resultMap type="com.aigt.code.entity.User" id="UserResult">
    </resultMap>

    <select id="getUserList" parameterType="com.aigt.code.entity.User" resultType="com.aigt.code.entity.User">
        select * from user where id =#{userId}
    </select>

</mapper>